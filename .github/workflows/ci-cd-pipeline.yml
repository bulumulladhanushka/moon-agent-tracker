name: CI/CD Pipeline

on:
  push:
    branches:
      - dev
  workflow_dispatch:

env:
  DOCKER_REPO: bulumulladhanu

jobs:
  build-and-push:
    name: Build and Push Docker Images
    runs-on: ubuntu-latest

    steps:
    - name: Checkout code
      uses: actions/checkout@v3

    - name: Log in to Docker Hub
      uses: docker/login-action@v2
      with:
        username: ${{ secrets.DOCKERHUB_USERNAME }}
        password: ${{ secrets.DOCKERHUB_TOKEN }}

    - name: Set up Docker Buildx
      uses: docker/setup-buildx-action@v2

    - name: Build and push Agent Service image
      uses: docker/build-push-action@v5
      with:
        context: ./agent-service
        push: true
        tags: ${{ env.DOCKER_REPO }}/agent-service:latest

    - name: Build and push Integration Service image
      uses: docker/build-push-action@v5
      with:
        context: ./integration-service
        push: true
        tags: ${{ env.DOCKER_REPO }}/integration-service:latest

    - name: Build and push Notification Service image
      uses: docker/build-push-action@v5
      with:
        context: ./notification-service
        push: true
        tags: ${{ env.DOCKER_REPO }}/notification-service:latest

  # Future job: Deploy to EKS using Blue-Green strategy
  # deploy:
  #   needs: build-and-push
  #   runs-on: ubuntu-latest
  #   steps:
  #     - name: Deploy to EKS ...
  #       run: echo "Deploy logic here"


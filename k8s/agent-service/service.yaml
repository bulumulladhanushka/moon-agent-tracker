apiVersion: v1
kind: Service
metadata:
  name: agent-service
spec:
  selector:
    app: agent-service
    version: blue  # initially route to 'blue', can be updated to 'green'
  ports:
    - protocol: TCP
      port: 80
      targetPort: 5000


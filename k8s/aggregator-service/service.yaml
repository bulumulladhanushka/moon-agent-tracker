apiVersion: v1
kind: Service
metadata:
  name: aggregator-service
spec:
  type: LoadBalancer
  selector:
    app: aggregator-service
    version: blue
  ports:
    - port: 5004
      targetPort: 5004
      protocol: TCP
